---
name: start
description: 项目启动流程
type: project
---

# Stock Assistant 启动流程

在同一 shell 中按顺序执行：

```bash
source ~/.nvm/nvm.sh && nvm use 20 && npm install && npm run dev
```

注意：必须写在同一行，确保 Node 版本切换生效。

启动后地址：
- Web: http://localhost:8888
- API: http://localhost:4000
